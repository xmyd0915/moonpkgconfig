# 更新记录

## 0.1.0 开发中 — 2026-09-16

首个实现里程碑：`.pc` 解析、顺序变量求值、字段来源、参数分词和依赖表达式解析。新增回归测试、演示和 MIT 许可证。依赖图和完整 CLI 尚未完成。

新增版本比较及依赖约束判断，覆盖数字段、字母段、分隔符、前导零和 `~` 预发布版本。

新增包级元数据校验，报告缺失、空值及包含空白的版本字段。

新增内存包集合和直接依赖检查，报告非法或重复标识符、缺失包及版本不满足。

新增传递依赖解析、私有依赖开关、稳定的依赖优先顺序及循环诊断。

新增编译与链接参数聚合，每个参数保留包、字段和源码位置；静态模式包含私有依赖。

新增文档、诊断与参数来源的 JSON 输出，可选紧凑或缩进格式。

依赖解析会检查已选包之间的 `Conflicts`，并保留冲突字段的来源位置。

依赖找不到同名包时可使用 `Provides` 声明的虚拟包，支持无版本查询和精确提供版本。

新增 `cmd/inspect`，可从磁盘读取 UTF-8 `.pc` 文件并输出字段摘要、来源、诊断或 JSON。

文件检查同时验证必填元数据，并用退出码区分检查通过、发现诊断及调用错误；仓库加入可复现的错误示例。

新增公开 CI，在全新 Ubuntu 环境执行格式检查、四目标检查与测试，并验证两条文件演示路径。

新增目录查询命令和三包示例，可从一组 `.pc` 文件解析传递依赖并输出动态、静态或带来源说明的参数。

新增整目录检查命令，一次汇总解析、元数据、公开和私有依赖、循环及冲突问题；目录加载逻辑由查询和检查命令共用。

查询命令新增可选的 `--dedupe-paths`，只去除重复编译和链接搜索路径，保留重复库及其余参数。

加入10个固定版本的pkgconf 3.0.7官方测试样本及ISC许可证，并在本地验证和Ubuntu CI中持续检查。

适配2026年9月20日MoonBit工具链新增的显式派生方法和测试包引用检查。

查询命令使用POSIX shell引用规则输出参数，保留包含空格、引号、美元符和空值的参数边界；结构化调用仍可使用JSON参数数组。
