name = "local/moonpkgconfig"

version = "0.1.0"

readme = "README.md"

license = "MIT"

description = "Source-aware pkg-config metadata parsing and native dependency inspection in MoonBit"

keywords = [ "pkg-config", "native", "ffi", "build-tools" ]

import {
  "moonbitlang/x@0.5.5",
}
